import { ApplicationInsightsOptions, LogInstrumentationsConfig } from "../../types";
export declare class JsonConfig implements ApplicationInsightsOptions {
    private static _instance;
    enableAutoCollectExceptions: boolean;
    logInstrumentations: LogInstrumentationsConfig;
    extendedMetrics: {
        [type: string]: boolean;
    };
    static getInstance(): JsonConfig;
    constructor();
    private _loadJsonFile;
}
